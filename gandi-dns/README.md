# Gandi LiveDNS Updater

A simple BASH script to update one or more subdomains on Gandi's LiveDNS

## Requirements

*jq `brew install jq`

## Usage

Set API_KEY, DOMAIN and SUBDOMAINS in the script and run it.
